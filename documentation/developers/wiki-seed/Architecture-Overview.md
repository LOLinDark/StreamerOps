# Architecture Overview

StreamerOps is split into four major areas:

- Frontend app: React + Vite UI and tooling surfaces
- Backend server: API endpoints and integration orchestration
- Overlay/relay sync: low-latency command flow plus state recovery
- Platform adapters: OBS and external service integration

This overview is intentionally lightweight and should be expanded as new architecture docs are added.
