export const HOTAS_MODES = ['green', 'orange', 'red'];

export const DEFAULT_MODE_STATE = {
  enabled: false,
  activeMode: 'green',
  updatedAt: null,
};

export const DEFAULT_BUTTON_MODE_BINDINGS = {
  button4: {
    green: 'z',
    orange: 'x',
    red: 'c',
  },
};
